# ---- Bonus dashboard dependencies (separate image/process) ----
streamlit==1.41.1
plotly==5.24.1
pandas==2.2.3
requests==2.32.3
# Orbit propagation for the "conjunction location" map (sub-satellite point at TCA).
skyfield==1.49
